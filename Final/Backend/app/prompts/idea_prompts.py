"""
idea_prompts — TODO(담당자): 여기에 프롬프트를 작성하세요.

작성 규칙:
  · 코드에 문자열을 직접 쓰지 말고 반드시 이 파일에 둘 것
  · common_prompts.with_rules() 로 감싸서 공통 규칙을 붙일 것

예:
    from app.prompts.common_prompts import with_rules

    WRITE_PROBLEM = with_rules('''
    아래 불편 사례들을 읽고 하나의 문제로 정의하라.
    ...
    ''')
"""

from app.prompts.common_prompts import with_rules  # noqa: F401
