# 뼈대 실행에 필요한 최소한
pydantic>=2.0
pydantic-settings>=2.2      # .env -> Settings (app/config/settings.py)
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0

# 외부 API 붙일 때
httpx>=0.27

# 에이전트 워크플로우 (담당자 정해지면 추가)
# langgraph>=0.2
# langchain-core>=0.3

# 묶기 (2 해석기) - TF-IDF char n-gram 기본안
scikit-learn>=1.5
numpy>=1.26

# 임베딩으로 전환할 때 (포텐스닷 임베딩 지원 여부 확인 후)
# sentence-transformers>=3.0

# 테스트
pytest>=8.0
respx>=0.21                 # httpx 목킹 (테스트가 실제 API를 못 부르게)
