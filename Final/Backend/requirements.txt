# 뼈대 실행에 필요한 최소한
pydantic>=2.0
fastapi>=0.110
uvicorn[standard]>=0.29
python-dotenv>=1.0

# 외부 API 붙일 때
httpx>=0.27

# 에이전트 워크플로우 (담당자 정해지면 추가)
# langgraph>=0.2
# langchain-core>=0.3

# 묶기를 임베딩으로 할 때 (방식 정해지면 추가)
# scikit-learn>=1.5
# numpy>=1.26
