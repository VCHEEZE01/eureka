'use client';
/**
 * 클라이언트 전역 상태.
 *
 * 담당 범위
 *   로그인(F10) · 리서치 범위(F02) · 문제 선택(F05)
 *   개인화 결과(F07) · 즐겨찾기/보관함(F09)
 *
 * 백엔드가 붙기 전까지는 localStorage 에 보관한다.
 * 붙은 뒤에도 "고른 범위" 같은 화면 상태는 여기 남는다.
 */
export {};  // TODO: 프론트 담당자가 구현
