'use client';
/**
 * 문제 상세 / 근거 확인 (PRD F04) — ★유일한 분기 지점
 *
 * 목적 : 문제가 실제로 반복되는지 판단할 근거 제공
 * 입력 : 문제 ID
 * 출력 : 설명 · 맥락 · 불편 요약 3~5건 · 출처 분포 · 비슷한 문제
 *
 * ★ 여기서 두 갈래로 갈린다
 *     [아이디어 보기]  → F06 기본 아이디어
 *     [개인화하기]     → F07 개인화
 *
 * ★ 필수 규칙 (PRD 8절)
 *   "실제 수집된 근거" 영역과 "AI가 생성한 설명" 영역을
 *   시각적으로 명확히 구분해서 표시할 것
 *   · 단일 검증점수는 표시하지 않는다
 *   · 근거 건수와 출처 수로 신뢰를 표현한다
 */
export function ProblemDetailScreen({ problemId }: { problemId: string }) {
  return null; // TODO: 프론트 담당자가 구현
}
