'use client';
/**
 * 에이전트 진행 상태를 받아 화면에 그리기 위한 훅.
 *
 * 쓰는 곳 : F02 리서치 범위 · F05 문제 조합 · F07 개인화
 *
 * 하는 일
 *   1. api/client.ts 의 함수를 실행한다
 *   2. 단계 알림(ProgressEvent)이 올 때마다 상태를 갱신한다
 *   3. 컴포넌트가 그걸 보고 진행 상태 UI를 다시 그린다
 *
 * 돌려줄 것
 *   steps      지금까지 알려진 단계 이름들
 *   current    현재 진행 중인 단계 번호
 *   doneSteps  완료된 단계 번호들
 *   loading    진행 중인가
 *   error      실패 사유 (PRD: 실패·시간초과 시 재시도 안내 필요)
 *   run(fn)    실행 시작
 */
export {};  // TODO: 프론트 담당자가 구현
