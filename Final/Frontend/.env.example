# 이 파일을 .env.local 로 복사해서 쓰세요. .env.local 은 깃에 안 올라갑니다.

# 백엔드 주소.
#   비워두면 → mock 데이터로 동작 (백엔드 없어도 화면이 다 돌아감)
#   채우면   → 진짜 백엔드에 붙음
NEXT_PUBLIC_API_BASE=

# 백엔드를 켰다면 보통 이 주소입니다.
# NEXT_PUBLIC_API_BASE=http://localhost:8000
