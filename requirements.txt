# Vercel 배포용 런타임 의존성 (진입점: index.py)
#
# ★ Final/Backend/requirements.txt 에서 서버 실행에 필요한 것만 옮긴 목록이다.
#   백엔드에 패키지를 추가하면 여기에도 같이 추가할 것 — 안 하면 로컬에선 되고
#   배포에서만 ModuleNotFoundError 가 난다.
#   (uvicorn·pytest·respx 는 Vercel 런타임에 필요 없어서 뺐다)
fastapi>=0.110
pydantic>=2.0
pydantic-settings>=2.2
python-dotenv>=1.0
httpx>=0.27

# Supabase 액세스 토큰 검증(ES256/HS256). [crypto]가 cryptography를 끌고 온다.
# 2026-09-20: idea-agent-v1 머지로 로그인/보관함 기능이 들어오며 추가.
# 이게 없으면 배포에서는 로그인 관련 API가 조용히 비활성화된다
# (app/core/auth.py가 import 실패를 서버 다운 없이 흡수하도록 짜여 있음).
PyJWT[crypto]>=2.9
